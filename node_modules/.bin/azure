#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../azure/bin/azure" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../azure/bin/azure" "$@"
  ret=$?
fi
exit $ret
